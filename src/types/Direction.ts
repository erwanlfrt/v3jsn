export enum KeyMapping {
  LEFT = 37,
  UP = 38,
  RIGHT = 39,
  DOWN = 40
}

export enum Reverse {
  LEFT = KeyMapping.RIGHT,
  RIGHT = KeyMapping.LEFT,
  UP =  KeyMapping.DOWN,
  DOWN = KeyMapping.UP
}